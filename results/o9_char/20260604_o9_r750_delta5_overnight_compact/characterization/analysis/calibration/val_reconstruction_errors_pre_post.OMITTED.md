# Omitted Large File

`val_reconstruction_errors_pre_post.csv` was not committed because it is a large raw validation table and exceeds GitHub size limits.

Keep compact summaries only for repository review.
