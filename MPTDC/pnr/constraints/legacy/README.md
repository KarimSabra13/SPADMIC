# Legacy PnR Constraints

Stable user-facing PnR SDC aliases are:

- `mptdc_typical_r750_delta5.sdc`
- `mptdc_clock_model_typical.sdc`
- `mptdc_phase_distribution.sdc`
- `mptdc_io_load_model.sdc`

Historical overlays remain in this directory's parent for compatibility until
all wrappers and server runbooks are validated against stable names.
